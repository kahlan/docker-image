# AUTOMATICALLY GENERATED
# DO NOT EDIT THIS FILE DIRECTLY, USE /Dockerfile-template.j2

FROM composer/composer:{{ composer_tag }}

MAINTAINER CrysaLEAD <contact@crysalead.com>


# Install Kahlan
RUN mkdir -p /usr/src/kahlan \
 && cd /usr/src/kahlan \
 && composer selfupdate \
 && composer require "kahlan/kahlan:{{ kahlan_ver }}" \
 && ln -s /usr/src/kahlan/vendor/bin/kahlan /usr/local/bin/kahlan \
 && ln -s /usr/local/bin/kahlan /kahlan


# Install Xdebug
{% if composer_tag[-6:] == 'alpine' -%}
RUN apk add --update --no-cache --virtual .tools-deps \
            autoconf g++ libtool make \
 && (yes | pecl install xdebug) \
 && apk del .tools-deps \
 && rm -rf /var/cache/apk/*
{%- else -%}
RUN yes | pecl install xdebug
{%- endif %}

# Create wrapper for running Kahlan under Xdebug
{% set ext_date = '20131226' if composer_tag[:4] == 'php5' else '20151012' -%}
RUN echo '#!/bin/sh'                         >> /usr/local/bin/kahlan-xdebug \
 && echo -n 'exec /usr/local/bin/php'        >> /usr/local/bin/kahlan-xdebug \
 && echo -n ' -dzend_extension=/usr/local/lib/php/extensions/no-debug-non-zts-{{ ext_date }}/xdebug.so' \
                                             >> /usr/local/bin/kahlan-xdebug \
 && echo ' -f /usr/local/bin/kahlan -- $@'   >> /usr/local/bin/kahlan-xdebug \
 && chmod +x /usr/local/bin/kahlan-xdebug \
 && ln -s /usr/local/bin/kahlan-xdebug /kahlan-xdebug


{%- if composer_tag[:4] != 'php5' %}


# Create wrapper for running Kahlan under phpdbg
RUN echo '#!/bin/sh' >> /usr/local/bin/kahlan-phpdbg \
 && echo 'exec /usr/local/bin/phpdbg -qrr /usr/local/bin/kahlan "$@"' \
                     >> /usr/local/bin/kahlan-phpdbg \
 && chmod +x /usr/local/bin/kahlan-phpdbg \
 && ln -s /usr/local/bin/kahlan-phpdbg /kahlan-phpdbg
{%- endif %}


ENTRYPOINT ["/kahlan"]
{{ '' }}
